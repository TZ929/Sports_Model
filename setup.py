from setuptools import setup, find_packages

setup(
    name='sports_model',
    version='0.1.0',
    description='A predictive model for sports betting.',
    author='Your Name',
    author_email='your.email@example.com',
    packages=find_packages(),
    install_requires=[
        # Add your project's dependencies here,
        # or read them from requirements.txt
    ],
) 